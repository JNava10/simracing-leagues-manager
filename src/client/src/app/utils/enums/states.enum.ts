export enum CreatingChampRoundStates {
  CreatingBasicInfo,
  CreatingTeams,
  CreatingScoreSystem,
  Overview,
  SharePresetConfig,
}

export enum EnteringChampStates {
  CreatingBasicInfo,
  CreatingGameName,
}
