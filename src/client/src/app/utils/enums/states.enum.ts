export enum CreatingChampStates {
  CreatingBasicInfo,
  CreatingTeams,
  CreatingScoreSystem,
  Overview,
  SharePresetConfig,
}

export enum CreatingEventStates {
  CreatingBasicInfo,
  CreatingTeams,
  Overview,
  SharePresetConfig,
}


export enum EnteringChampStates {
  CreatingBasicInfo,
  CreatingGameName,
}
